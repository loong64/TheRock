name: Publish build_manylinux_rccl_loongarch64 images
on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
      - 'stage/docker/**'
    paths:
      - dockerfiles/build_manylinux_rccl_loongarch64*.Dockerfile
      - .github/workflows/publish_build_manylinux_rccl_loongarch64.yml

permissions:
  contents: read
  packages: write

jobs:
  publish_build_manylinux_loongarch64:
    uses: ./.github/workflows/publish_dockerfile.yml
    with:
      DOCKER_FILE_NAME: build_manylinux_rccl_loongarch64
      DOCKER_IMAGE_NAME: therock_build_manylinux_rccl_loongarch64
